export interface FollowUser {
    userId: string;         // 사용자 ID
    name: string;           // 사용자 이름
    role: string;           // 사용자 역할 (예: STUDENT, PROFESSOR 등)
    profileImageUrl: string; // 프로필 이미지 URL
}
